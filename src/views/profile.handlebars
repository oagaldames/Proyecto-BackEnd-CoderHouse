{{!-- <div>
    <h1>Tu perfil (sin datos sensibles)</h1>
    <p>{{user.name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.age}}</p>
    <a href="/api/sessions/logout"><button>Salir</button></a>
</div> --}}

<html>
  <head>
    <title>Tu perfil</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
  </head>
  <body>
    <div class="container">
      <div class="card p-4 mt-4">
        <h1 class="text-center">Tu perfil (sin datos sensibles)</h1>
        <p class="mb-2"><strong>Nombre:</strong> {{user.name}}</p>
        <p class="mb-2"><strong>Email:</strong> {{user.email}}</p>
        <p class="mb-2"><strong>Edad:</strong> {{user.age}}</p>
        <a href="/api/sessions/logout" class="btn btn-primary">Salir</a>
      </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script
      src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"
    ></script>
    <script
      src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"
    ></script>
  </body>
</html>